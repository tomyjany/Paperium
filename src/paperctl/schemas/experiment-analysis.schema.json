{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://paperctl.local/schemas/experiment-analysis.schema.json",
    "title": "paperctl experiment analysis",
    "type": "object",
    "additionalProperties": false,
    "required": [
        "schema_version",
        "artifact_type",
        "question_path",
        "experiment_path",
        "title",
        "execution_status",
        "hypothesis_verdict",
        "objective",
        "answer",
        "meaning",
        "limitations",
        "confidence",
        "claims"
    ],
    "properties": {
        "schema_version": {"type": "integer", "const": 1},
        "artifact_type": {"type": "string", "const": "experiment_analysis"},
        "question_path": {"$ref": "#/$defs/relative_path"},
        "experiment_path": {"$ref": "#/$defs/relative_path"},
        "title": {"type": "string", "minLength": 1, "maxLength": 160},
        "execution_status": {
            "type": "string",
            "enum": ["completed", "failed", "incomplete", "unknown"]
        },
        "hypothesis_verdict": {
            "type": "string",
            "enum": [
                "supported",
                "partially_supported",
                "not_supported",
                "inconclusive",
                "not_applicable"
            ]
        },
        "objective": {"type": "string", "minLength": 1, "maxLength": 1000},
        "answer": {"type": "string", "minLength": 1, "maxLength": 1500},
        "meaning": {"type": "string", "minLength": 1, "maxLength": 1500},
        "limitations": {
            "type": "array",
            "maxItems": 10,
            "items": {"type": "string", "minLength": 1, "maxLength": 500}
        },
        "confidence": {"type": "string", "enum": ["high", "medium", "low"]},
        "claims": {
            "type": "array",
            "maxItems": 50,
            "items": {"$ref": "#/$defs/claim"}
        }
    },
    "$defs": {
        "hash": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
        "relative_path": {
            "type": "string",
            "minLength": 1,
            "maxLength": 1000,
            "pattern": "^[^\\\\]+$"
        },
        "claim_id": {
            "type": "string",
            "minLength": 1,
            "maxLength": 80,
            "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
        },
        "label": {"type": "string", "minLength": 1, "maxLength": 160},
        "unit": {
            "anyOf": [
                {"type": "string", "minLength": 1, "maxLength": 80},
                {"type": "null"}
            ]
        },
        "json_pointer": {
            "type": "string",
            "maxLength": 1000,
            "pattern": "^($|/.*)$"
        },
        "source": {
            "type": "object",
            "additionalProperties": false,
            "required": ["path", "source_hash", "selector_type", "selector"],
            "properties": {
                "path": {"$ref": "#/$defs/relative_path"},
                "source_hash": {"$ref": "#/$defs/hash"},
                "selector_type": {"type": "string", "const": "json_pointer"},
                "selector": {"$ref": "#/$defs/json_pointer"}
            }
        },
        "scalar": {
            "anyOf": [
                {"type": "string", "maxLength": 2000},
                {"type": "number"},
                {"type": "integer"},
                {"type": "boolean"},
                {"type": "null"}
            ]
        },
        "value_type": {
            "type": "string",
            "enum": ["string", "number", "integer", "boolean", "null"]
        },
        "numeric_value_type": {"type": "string", "enum": ["number", "integer"]},
        "measured_claim": {
            "type": "object",
            "additionalProperties": false,
            "required": [
                "claim_id",
                "claim_type",
                "label",
                "value",
                "value_type",
                "unit",
                "source"
            ],
            "properties": {
                "claim_id": {"$ref": "#/$defs/claim_id"},
                "claim_type": {"type": "string", "const": "measured_value"},
                "label": {"$ref": "#/$defs/label"},
                "value": {"$ref": "#/$defs/scalar"},
                "value_type": {"$ref": "#/$defs/value_type"},
                "unit": {"$ref": "#/$defs/unit"},
                "source": {"$ref": "#/$defs/source"}
            }
        },
        "derived_claim": {
            "type": "object",
            "additionalProperties": false,
            "required": [
                "claim_id",
                "claim_type",
                "label",
                "value",
                "value_type",
                "unit",
                "formula",
                "input_claim_ids"
            ],
            "properties": {
                "claim_id": {"$ref": "#/$defs/claim_id"},
                "claim_type": {"type": "string", "const": "derived_value"},
                "label": {"$ref": "#/$defs/label"},
                "value": {"type": "number"},
                "value_type": {"$ref": "#/$defs/numeric_value_type"},
                "unit": {"$ref": "#/$defs/unit"},
                "formula": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 300,
                    "pattern": "^[\\u0000-\\u007f]+$"
                },
                "input_claim_ids": {
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 20,
                    "items": {"$ref": "#/$defs/claim_id"}
                }
            }
        },
        "claim": {
            "anyOf": [
                {"$ref": "#/$defs/measured_claim"},
                {"$ref": "#/$defs/derived_claim"}
            ]
        }
    }
}
